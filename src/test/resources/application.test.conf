http {
  interface = "localhost"
  port = 8080
}
db {
  url = "jdbc:postgresql://localhost:5432/postgres"
  url = ${?DB_URL}
  password = "CitizenErased1"
}
akka {
  persistence {
    journal {
      plugin = "akka.persistence.journal.inmem"
    }
    snapshot-store {
      plugin = "akka.persistence.snapshot-store.local"
      local.dir = "target/snapshots"
    }
  }
}
